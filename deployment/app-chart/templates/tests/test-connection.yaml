apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sputnik-v1-api.fullname" . }}-test-connection"
  labels:
    {{- include "sputnik-v1-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sputnik-v1-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
