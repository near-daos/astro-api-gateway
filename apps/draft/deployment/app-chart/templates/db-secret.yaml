apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: documentdb-secret
spec:
  dataFrom:
  - extract:
      conversionStrategy: Default
      key: Astro{{ include "aws.env" . }}DraftDocumentdbSecrets
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: documentdb-secret
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: documentdb-secret
