apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: redis-secret
spec:
  dataFrom:
  - extract:
      conversionStrategy: Default
      key: Astro{{ include "aws.env" . }}ApiRedisSecrets
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: redis-secret
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: redis-secret
